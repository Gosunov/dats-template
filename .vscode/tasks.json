{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Mock API",
            "type": "shell",
            "command": "./client --local",
        },
        {
            "label": "Server API",
            "type": "shell",
            "command": "./client --server gosunov.ru --token 123",
        }
    ]
}